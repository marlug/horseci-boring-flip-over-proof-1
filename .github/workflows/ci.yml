name: CI
on:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run a script
        run: |
          echo "Hello from the boring proof."
          ls -la
          echo "Done."
